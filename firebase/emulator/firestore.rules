rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive emulator-only rules. Production rules live in the deployed project.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
