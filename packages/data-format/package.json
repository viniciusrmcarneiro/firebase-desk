{
  "name": "@firebase-desk/data-format",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "oxlint --config ../config-oxlint/oxlintrc.base.json src",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@firebase-desk/config-oxlint": "workspace:*",
    "@firebase-desk/config-tsconfig": "workspace:*",
    "@firebase-desk/config-vitest": "workspace:*",
    "oxlint": "1.61.0",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
